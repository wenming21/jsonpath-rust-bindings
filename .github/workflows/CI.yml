# This file is autogenerated by maturin>=v1.10.0,<2.0
# To update, run
#
#    maturin generate-ci github
#
# After all Python versions are installed, the --find-interpreter parameter of maturin build still fails to
# find all Python versions, resulting in some Python versions not being built. Therefore, the -i parameter
# is needed to manually specify Python versions.
#

name: CI

on:
  push:
    branches:
      - main
      - master
    tags:
      - '*'
    paths-ignore:
      - '**.md'
  pull_request:
    paths-ignore:
      - '**.md'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # Build Jobs
  # These jobs will build the wheels for all Python versions and upload them as artifacts.
  linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [x86_64, x86, aarch64, armv7, s390x, ppc64le]
    steps:
      - uses: actions/checkout@v7

      - uses: Swatinem/rust-cache@v2.9.2

      - name: Build wheels for all Python versions
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: |
            --release
            --out target/wheels
            -i python3.8 python3.9 python3.10 python3.11 python3.12 python3.13 python3.14 python3.15 pypy3.11
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
          manylinux: auto

      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-linux-${{ matrix.target }}
          path: target/wheels

  musllinux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [x86_64, x86, aarch64, armv7]
    steps:
      - uses: actions/checkout@v7

      - uses: Swatinem/rust-cache@v2.9.2

      - name: Build wheels for all Python versions
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: |
            --release
            --out target/wheels
            -i python3.8 python3.9 python3.10 python3.11 python3.12 python3.13 python3.14 python3.15 pypy3.11
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
          manylinux: musllinux_1_2

      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-musllinux-${{ matrix.target }}
          path: target/wheels

  windows-x64:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v7

      - uses: Swatinem/rust-cache@v2.9.2

      - name: Install UV
        uses: astral-sh/setup-uv@v10.1.0
        with:
          enable-cache: true

      - name: Install all Python versions
        run: uv python install 3.8 3.9 3.10 3.11 3.12 3.13 3.14 3.15 pypy3.11

      - name: Build wheels for all Python versions
        uses: PyO3/maturin-action@v1
        with:
          target: x64
          args: |
            --release
            --out target/wheels
            -i python3.8 python3.9 python3.10 python3.11 python3.12 python3.13 python3.14 python3.15
            -i ${{ env.PYPY_PATHS }}
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}

      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-windows-x64
          path: target/wheels

  windows-x86:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v7

      - uses: Swatinem/rust-cache@v2.9.2

      - name: Install UV
        uses: astral-sh/setup-uv@v10.1.0
        with:
          enable-cache: true

      - name: Install all Python versions
        run: >
          uv python install
          cpython-3.8-windows-i686-none
          cpython-3.9-windows-i686-none
          cpython-3.10-windows-i686-none
          cpython-3.11-windows-i686-none
          cpython-3.12-windows-i686-none
          cpython-3.13-windows-i686-none
          cpython-3.14-windows-i686-none
          cpython-3.15-windows-i686-none

      - name: Build wheels for all Python versions
        uses: PyO3/maturin-action@v1
        with:
          target: x86
          args: |
            --release
            --out target/wheels
            -i python3.8 python3.9 python3.10 python3.11 python3.12 python3.13 python3.14 python3.15
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}

      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-windows-x86
          path: target/wheels

  windows-arm64:
    runs-on: windows-11-arm
    steps:
      - uses: actions/checkout@v7

      - uses: Swatinem/rust-cache@v2.9.2

      - name: Build wheels for all Python versions
        uses: PyO3/maturin-action@v1
        with:
          target: aarch64-pc-windows-msvc
          args: |
            --release
            --out target/wheels
            -i python3.11 python3.12 python3.13 python3.14 python3.15
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}

      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-windows-arm64
          path: target/wheels

  macos:
    runs-on: macos-latest
    strategy:
      matrix:
        target: [x86_64, aarch64]
    steps:
      - uses: actions/checkout@v7

      - uses: Swatinem/rust-cache@v2.9.2

      - name: Build wheels for all Python versions
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: |
            --release
            --out target/wheels
            -i python3.8 python3.9 python3.10 python3.11 python3.12 python3.13 python3.14 python3.15 pypy3.11
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}

      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-macos-${{ matrix.target }}
          path: target/wheels

  sdist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out target/wheels
      - name: Upload sdist
        uses: actions/upload-artifact@v7
        with:
          name: wheels-sdist
          path: target/wheels

  release:
    name: Release
    runs-on: ubuntu-latest
    if: ${{ startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'}}
    needs: [linux, musllinux, windows-x64, windows-x86, windows-arm64, macos, sdist]
    permissions:
      # Use to sign the release artifacts
      id-token: write
      # Used to upload release artifacts
      contents: write
      # Used to generate artifact attestation
      attestations: write
    steps:
      - uses: actions/download-artifact@v7
      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: 'wheels-*/*'
      - name: Publish to PyPI
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        uses: PyO3/maturin-action@v1
        env:
          MATURIN_PYPI_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        with:
          command: upload
          args: --non-interactive --skip-existing wheels-*/*
